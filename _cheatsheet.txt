# Git
https://github.com/joshnh/Git-Commands
git clone https://github.com/[username]/[repository-name].git
git status
git add [file-name.txt]
git add -A
git commit -m '[commit-message]'
git rm -r [file-name.txt]
git push
git pull
git stash
git stash clear
git stash apply
git ls-files | xargs wc -l # count lines of code
git ls-files | grep '\.py' | xargs wc -l # count lines of python code
git config --global http.postBuffer 524288000 # increase size of http buffer

# Screen
http://aperiodic.net/screen/quick_reference
screen -ls
screen -S [session-name]
screen -x
screen -r [session-name]
ctrl-a d
ctrl-a h # toggle logging
ctrl-a ctrl-g # toggle visual bell
mv hardcopy.0 attention/logs/session_log.txt # move log
exit

# Conda
conda create -n [env-name] python=3.6
conda activate [env-name]
conda install [package1] [package2] ...
conda install cython ipython numpy scipy pandas matplotlib scikit-learn scikit-image opencv
conda install pytorch torchvision cudatoolkit=10.0 -c pytorch tqdm
pip install keras tensorflow-gpu
conda deactivate

# Bash
http://rik.smith-unna.com/command_line_bootcamp/?id=eto0hlhxx8p
https://www.geeksforgeeks.org/du-command-linux-examples/
ssh [username]@love16.pals.ucl.ac.uk
sshfs freddie@love16.pals.ucl.ac.uk:/home/freddie /Users/fbickfordsmith/love16
sshfs [username]@love16.pals.ucl.ac.uk:/path-on-server/ ~/path-to-mount-point
exit # disconnect
watch -n1.0 nvidia-smi
free -h # memory
du -sh [folder-path] # show human-readable summary
du -ah [folder-path] | tail # show files as well as folder
python3 -i [file-name.py] # run in interactive mode
top -u [username] # get process IDs (PIDs)
kill -9 [PID] # kill process
top -b -n 1 -u freddie | awk 'NR>7 { sum += $9; } END { print sum; }'
top -b -n 1 -u [username] | awk 'NR>7 { sum += $9; } END { print sum; }' # total user cpu usage
glances # similar to top

# Managing files
https://everythinglinux.org/rsync/
pwd, cd, ls, mkdir, rmdir, rm
rm -rf [folder-name] # remove folder and files
cp [src-file-path] [dest-file-path] # copy file
cp -r [src-folder-path] [dest-folder-path] # copy folder
rsync --progress --recursive '/Users/fbickfordsmith/love16/activations/' '/Users/fbickfordsmith/activations-copy/'
rsync --progress --compress [src-file-path] [dest-folder-path] # copy file
mv [src-file-path] [dest-file-path] # move/rename
ls -1 | wc -l # count files

# Copying a folder
old_folder contains a, b, c
With no slash:
    cp -r .../old_folder .../new_folder => new_folder contains old_folder
With slash:
     cp -r .../old_folder/ .../new_folder => new_folder contains a, b, c
Example:
    cp -r /mnt/fast-data16/datasets/ILSVRC/2012/clsloc/ /home/freddie/ILSVRC2012-simcontexts

# Python
sys.path.insert(0, [path-to-files])
glob.iglob(root_dir+'/**', recursive=True) returns an iterator (arbitrary order)
glob.glob(root_dir+'/**', recursive=True) returns a list (use sorted() to sort)
